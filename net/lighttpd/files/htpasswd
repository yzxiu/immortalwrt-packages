# lighttpd plain auth backend user file
# Format:  username:password   (plain text)
# Change the default password below to your own, then restart lighttpd:
#   /etc/init.d/lighttpd restart
admin:change-me
